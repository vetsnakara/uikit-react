import { Meta } from "@storybook/addon-docs";
import logo from "./logo.svg";

<Meta title="PRR" parameters={{ previewTabs: { canvas: { hidden: true } } }} />

<img src={logo} />

# Использование UIKit React на ПРР

-   что изменено в репозитории ???
-   удалено все от базового UIKit
-   webpack 5 для SB -> команды сборки (env), querystring, конфиг сборки
-   перенести MCHD из PRR -> UIKit React repo
